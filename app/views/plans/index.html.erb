<div class="container mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold text-center mb-6">プラン一覧</h1>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <% @public_plans.each do |plan| %>
      <%= render 'plans', plan: plan %>
    <% end %>
  </div>
</div>
