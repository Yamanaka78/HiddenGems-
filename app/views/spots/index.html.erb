<div class="flex min-h-screen">
  <!-- サイドバー -->
  <%= render "spots/search_sidebar", q: @q %>

  <!-- メインコンテンツ -->
  <main class="flex-1 p-8">
    <!-- ヘッダー -->
    <%= render "spots/header" %>

    <!-- スポットリスト -->
    <%= render "spots/spot_list", spots: @spots %>
  </main>
</div>
