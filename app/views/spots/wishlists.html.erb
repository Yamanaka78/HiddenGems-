<div class="py-8">
  <section class="max-w-5xl mx-auto bg-white shadow-md rounded-lg p-6">
    <div class="mb-6">
      <h1 class="text-2xl font-semibold text-gray-800">お気に入り</h1>
    </div>

    <!-- Wishlist Items -->
    <% if @wishlists.present? %>
      <ul class="space-y-6">
        <%= render @wishlists %>
      </ul>
    <% else %>
        <p><%= t('.no_result') %></p>
    <% end %>
  </section>
</div>
